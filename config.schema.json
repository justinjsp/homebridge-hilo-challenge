{
  "pluginAlias": "HiloChallenge",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "⚠️ **Requires the [homebridge-hilo](https://github.com/johannrichard/homebridge-hilo) plugin.** This plugin reuses the refresh token from homebridge-hilo — install and configure that plugin first, then copy its `refreshToken` value into the field below.",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "default": "Hilo Challenge",
        "description": "Platform name shown in the Homebridge logs."
      },
      "refreshToken": {
        "title": "Hilo Refresh Token",
        "type": "string",
        "description": "Your Hilo refresh token — copy this from your homebridge-hilo plugin config."
      },
      "locationId": {
        "title": "Location ID (optional)",
        "type": "string",
        "description": "Your Hilo location URN. Found in homebridge-hilo logs: 'Subscribed to updates for location urn:hilo:crm:...'  Leave blank to auto-discover."
      },
      "pollInterval": {
        "title": "Poll Interval (seconds)",
        "type": "integer",
        "default": 60,
        "minimum": 30,
        "maximum": 300
      }
    },
    "required": ["name", "refreshToken"]
  }
}
